# 2026-09-14

## 1. ブレインの有用性を測り、読む側の門・土台・指標を入れる

**経緯:** 本人から「今のブレインは有用か、ゴミ置き場になっていないか」。印象でなく数えた結果、
書く側の門（還元行）は回っているが読む側の実績がほぼ無い（報告書 123 本のうち「再発:」1 本、
09-11 以降の還元行 10 本のうち型を名指しで参照 2 本）。書き方の汎用性（値を確認項目にする・
50 行以内・固有名の漏れ 1 箇所）は概ね守れていた。本人の採否: 提案した順番で直す。

**① 読む側の門（中央 + このリポジトリ）:**
- 中央 `check_return_line.py` を 2 段階に。09-11 以降は還元行の有無、**09-14 以降は「参照した型」欄に
  索引を引いた証拠**（型の名前か `当たり無し（grep: 引いた語）`）を要求。素の「無し」は落ちる
- 書式の定義元 5 か所を同じ文法に更新（共通 CLAUDE.md・中央 README・指示書・このリポジトリの
  history/README と CLAUDE.md）
- `frontend/tests/history-return-line.test.ts` に同じ文法を移植（CI から中央スクリプトは届かないため。
  文法テスト 6 ケースも同居）。変異テスト: 還元行なし／素の無し／grep 語なし → vitest・中央とも落ちる、
  名前あり／当たり無し（grep: 語） → 両方通る

**② 土台（中央）:** 22 本のアプローチに 2〜5 本ずつ複製されていた原則を `approaches/土台.md` の
10 か条に抜き出し、9 本の本文を「（土台 N）」の参照に置き換えた。`check_brain.sh` に再掲の警告を追加。

**③ 指標（中央）:** `brain_metrics.py` を新設し、README に月次の指標表と判定規則を置いた。
基準線（2026-09-14）: 報告書 123 / アプローチ 23 / 再発 1 / 名指し参照 2/10 / 引かれた型 2 種 /
固有名の漏れ 1 / 再掲 0。**次回 2026-10-14。再発と名指し参照が増えていなければ報告書の新規を止め、
アプローチだけに絞る。**

**主な変更ファイル:** `frontend/tests/history-return-line.test.ts`、`history/README.md`、`CLAUDE.md`。
中央: `check_return_line.py`、`check_brain.sh`、`brain_metrics.py`（新規）、`approaches/土台.md`（新規）+
9 本更新、`APPROACHES.md`、`README.md`、`指示書_型の棚卸しと還元.md`、`~/.claude/CLAUDE.md`

**未解決課題:** 他プロジェクトの CI 用複製（philosophybook・Learning_Bulletin_Board は中央スクリプトを
直接呼ぶので追従不要）。`検査の層と門.md` が 59 行で目安の 50 行を超えている（次の統合時に圧縮）。


## 2. 思想「開始時に聞き切る」を中央に明文化し、開始時の質疑応答と門を作る

**経緯:** 本人「究極の理想はプロジェクト開始時に質疑応答を済ませ一発目で完成させること。机上論と
分かったうえで、その思想が大事」。現行は「作業の種類ごと」「症状」の 2 入口で、開始時の入口が無く、
着手前の質問約 100 問が 23 ファイルに散っていた。トラブル 123 件のうち事前の質問へ変換済みは 0。

**中央に入れたもの:**
- README 冒頭に「思想」節（究極の形・変換の義務・3 つの入口）
- `kickoff_questions.py`: 各アプローチの「確認・提案すること」から `開始時の質疑応答.md` を段階別に生成
  （質問の正本はアプローチ側。生成物は check_brain が鮮度を見る）
- `check_kickoff.py`: 各 PJ の `brain/PREMISES.md` の「開始時の質疑応答」表が全アプローチを 済/該当せず で
  埋めているかを見る門（check_brain では警告、PJ 側で exit 1 に配線できる）
- TEMPLATE に「事前の質問」欄（09-14 以降必須。check_brain が警告）、`指示書_報告書を事前の質問に変換.md`
- 報告書 10 件（77・78・97・17・113・121・91・106・107・109）を変換。新しい質問 4 問を
  検査の層と門・Web アプリをストアへ・体感の遅さ・報告の裏取り に収容、6 件は既存の行を指す、1 件は「聞いても防げない」
- 指標に「事前の質問へ変換」「質疑応答を答え切った PJ」を追加

**このリポジトリ:** `brain/PREMISES.md` に質疑応答の表（22 行）。**未回答 3 本**: 不変条件を門に落とす
（表が無い）、検査の層と門（層の一覧と状態行列が無い）、Web アプリをストアへ出す（要件の原文・実機・予告表示）。
門はこの 3 本で落ちる状態を正として残す（答えるのは本人と一緒に）。

**主な変更ファイル:** `brain/PREMISES.md`、本ファイル。中央: README・TEMPLATE・check_brain.sh・brain_metrics.py・
kickoff_questions.py（新）・check_kickoff.py（新）・開始時の質疑応答.md（生成）・指示書（新）・approaches 4 本・reports 10 件

**未解決課題:** 残り 113 件の報告書の変換（指示書に沿って 10〜15 件ずつ）。tarot の未回答 3 本。
次の新規プロジェクトで質疑応答を開始時に通し、着手後に出た問題のうち事前に防げたはずの割合を測る。

## 3. 思想の補正: ブレインが持つのは「答え」でなく「詰めるべき決定項目」

**経緯:** 本人「前回こうしたから次回もそうするのが正解ではない。詰める必要がある点が学びとして活きる」。
点検すると、開始時の質疑応答 104 問のうち 8 問に「既定は」「推奨」が焼き込まれ、8 問が「〜してよいか」型で
答えを前提にしていた。前回の判断を日付つきの別枠で添えた問いは 0。

**中央に入れたもの:** README 思想節に「答えでなく決定項目。前回の値は一例で正解ではない」を追記。土台 7 に
質問の書式（開いた形／前回は `前回: X（日付・理由）` の別枠／既定・推奨・してよいか を書かない）。
9 本のアプローチの 14 問を書き直し（2 問は DB の既定 GRANT・モデルの既定継承という用語で誤検出のため据え置き）。
生成物は「前回:」を字下げして「一例。今回の正解ではない」と描く。`check_brain.sh` に焼き込みの警告を追加し、
わざと焼き込んだ行で警告が出ることを確認。変換の指示書と棚卸しの指示書にも同じ書式規則を追記。

**主な変更ファイル:** 本ファイルのみ（中央: README・土台・approaches 9 本・kickoff_questions.py・check_brain.sh・指示書 2 本）

## 4. 報告書の変換 1 バッチ + tarot の未回答 2 本を詰める

**中央:** 未参照だった報告書 15 件（01〜11・26・27・31・35〜37）を事前の質問へ変換。収容先が無かった
「配信先・初回デプロイ・ローカルとの差・確認スクリプト・ポート」の質問群のために `approaches/配信先と実行環境を決める.md`
を新設（6 問）。既存へは 2 問追加（検査の層と門: 0 件を失敗にするか／本番データ投入: 投入の分割）。1 件は聞いても防げない。
変換率 10 → 25/123、開始時の質疑応答は 112 問。

**このリポジトリ:** 開始時の質疑応答で「未」だった 2 本に答えた。`docs/検査と門.md` を新設し、不変条件 17 行（門なし 6 行を
明記）と検査の層 8 層（画面・使いやすさ・ネイティブは穴）を表にした。表が名指しするファイルの実在を
`frontend/tests/docs-gates.test.ts` が見る（変異テスト済: 存在しないパスを足すと落ちる）。CLAUDE.md に一次資料として追記。
PREMISES の表を 済 に更新し、新設アプローチの行も追加。**残る未回答は「Web アプリをストアへ出す」1 本**（実機と原文確認が要る）。

**検証:** テスト 123 件緑・lint 0。中央 check_brain エラー 0。

**主な変更ファイル:** `docs/検査と門.md`（新規）、`frontend/tests/docs-gates.test.ts`（新規）、`CLAUDE.md`、`brain/PREMISES.md`。
中央: `approaches/配信先と実行環境を決める.md`（新規）、approaches 2 本、reports 15 件、`kickoff_questions.py`、`APPROACHES.md`

**未解決課題:** 報告書の変換 残り 98 件。`docs/検査と門.md` のバックログ（#7 のフラグ直書き grep テストが最も安い）。
ストアの 3 点（本人と一緒に）。

## 5. 報告書 123 件の変換を完了、質疑応答 166 問に

**中央:** 残り 98 件を 7 体の担当に 14 件ずつ読ませ（読むだけ・JSON で提案）、私が重複・焼き込み・収容先を点検して
一括反映。内訳: 質問へ 71 件（うち既存の行を指すだけ 17 件）、聞いても防げない 27 件。新設 2 本
（`画面と入力の設計で先に決めること` 12 問、`無料枠・プラン・費用の確認` 3 問）。既存 12 本に 40 問追加。
変換率 25 → **123/123**、開始時の質疑応答 112 → **166 問**、アプローチ 26 本。check_brain エラー 0・焼き込み 0。

**このリポジトリ:** 機能フラグの集約テスト（検査と門 #7 → あり）。PREMISES に新設 2 本の行を足し、正直に「未」
（画面幅・色の基準・押した直後の表示／無料枠の商用条件の原文確認）。**質疑応答の未回答は 3 本**（ストア・画面と入力・無料枠）。

**判断の上書き（担当の提案から変えた点）:** 「入力検証の方針」の新設提案（2 問）は採らず画面側 12 問と合流。
検査の層と門に集まった設計時の決定 4 問を画面側へ移動。費用 3 問を配信先から分離。

**主な変更ファイル:** `frontend/tests/features-flag.test.ts`、`docs/検査と門.md`、`brain/PREMISES.md`。中央: reports 98 件、
approaches 2 新設 + 12 更新、`kickoff_questions.py`、`APPROACHES.md`、`開始時の質疑応答.md`

**未解決課題:** `検査の層と門.md` が 69 行（組み方と運用に分ける候補）。tarot の未回答 3 本（本人と一緒に）。
次の新規プロジェクトで 166 問を開始時に通し、「着手後に出た問題のうち事前に防げたはずの割合」を測る。

## 6. 残しを無くす: 未回答 3 本を閉じ、肥大したアプローチを分割

**本人指示:** 「残作業ある段階にしないで。全部やり切って」。

**tarot の未回答 3 本 → 済:**
- ストア要件を原文で確認して PREMISES の決定欄に記録（Apple 年 99 米ドル・個人は本名が販売元／Google Play 12 人×14 日）。
  「予告表示」を仕様 §3.3 に定義（未提供機能をほのめかす語を置くこと。無料版はどの画面にも置かない）。実機確認は
  決定済みの工程として未決欄に残す（Android Studio は本人作業）
- 無料枠を原文で確認（Cloudflare Pages Free の制限ページ 2026-09-05 版を直接取得。WebFetch は 3 回タイムアウトしたため
  curl + UA で取得＝中央 `reports/09` の型。Sentry Developer プラン）。見直し時期を決めた
- 画面と入力の設計は現状の実装から読み取り、コントラストを実測（全組み合わせ 4.5:1 以上）して決定として固定

**中央:** `検査の層と門.md`（69 行）を組み方（58 行）と `検査を安定させる.md`（35 行、新設）に分割。索引・段階を更新。

**触らなかったもの（残作業ではなく対象外）:** 他 6 プロジェクトの質疑応答の表。各プロジェクトのセッションで、その
リポジトリの現物を読んで埋めるもの（社用は読み取り専用の規則もある）。中央 check_brain が警告で見続ける。

**検証:** check_kickoff 0 件、テスト緑、中央エラー 0、指標「答え切った PJ」1/7。

## 7. tarot の穴埋め: 描画テスト 4 画面（独立設計）・仕様 lint・サムネイル化

**本人指示:** ストアは保留。私ができる範囲で。

**1. 描画テスト 4 画面（規模「中」、不具合修正扱い。仕様書は変えない）:** 仕様 §5.1/5.4/5.5/5.6 の文面と
公開契約だけを別担当に渡して 41 件を設計（src 不参照・コマンド不実行）。初回 38 件中 8 件失敗を切り分け:
- **実装の不備 3 件（直した）:** ①結果画面の idle リダイレクトが毎描画で走り、reset → navigate と競走して
  「もう一度占う」がトップへ着く。マウント時判定に変更。エラー状態の「トップへ戻る」に reset 追加
  ②カード詳細の not found が英語 `Card not found` のまま → 日本語固定文言 ③`?orientation=` が片面表示 →
  仕様どおり両面・指定側を先・画像は逆位置で回転（`brain/reports/06`）
- **テスト環境の穴 1 件（直した）:** jsdom で framer-motion の `onAnimationComplete` が発火せず（5 秒待っても）、
  めくり完了に依存する拡大ボタンが有効にならない → `tests/setup.ts` で `MotionGlobalConfig.skipAnimations = true`
- **契約の書き漏れ 1 件（設計へ差し戻し）:** キーワードは 1 語ずつ `<li>`。担当が 3 ファイルを直して 41 件全緑
- 設計側の「仕様への質問」9 件に回答（confirm の文言・シェアのファイル名と本文・error の接頭辞など）。
  仕様に載せるべきものは無し

**2. 仕様 lint:** `tests/spec-lint.test.ts`（§5.1 ルーティング表 ↔ App.tsx、§4.3 キーの実在、§6 の差分行に日付）。
検査と門 #11 を「一部」に。変異テスト済（表に架空のルートで落ちる）

**3. サムネイル:** 小さく描く 4 箇所（一覧・履歴・本日の一枚・山札）を 320px 幅の `public/cards/thumbs/` に。
23 枚 9.3MB → 1.1MB。対応は `lib/api.ts` の `thumbUrl()` のみ。`tests/card-thumbs.test.ts` で固定。
`pnpm gen:thumbs`（Pillow）。PREMISES の未決から「裏面画像の解像度」を消して決定へ

**検証:** テスト 174 件（+48）・lint 0・本番ビルド緑。検査と門: 描画層「あり」（全 7 画面）、#18・#19 追加

**主な変更ファイル:** `src/pages/ResultPage.tsx`・`CardDetailPage.tsx`・`CardListPage.tsx`・`HistoryPage.tsx`、
`src/components/TodaysCard.tsx`、`src/pages/ShufflePage.tsx`、`src/lib/api.ts`、`tests/setup.ts`、
`tests/components/{ResultPage,CardListPage,CardDetailPage,HistoryPage}.test.tsx`、`tests/spec-lint.test.ts`、
`tests/card-thumbs.test.ts`、`scripts/gen-thumbs.py`、`public/cards/thumbs/`、`docs/検査と門.md`、`brain/reports/06`

**未解決課題:** 無し（ストア申請とAndroid 実機は本人判断で保留）。


ブレイン: 参照した型 `仕様書駆動で挙動を変える.md`（独立設計・3 種の切り分け・契約の書き漏れは設計へ戻す）・`検査の層と門.md`（描画層の門）・`不変条件を門に落とす.md`（門は変異テストまで）・`報告の裏取り.md`（評価を印象でなく数字で） / 適用前に確認した値 無し（順番①②③は本人が採否） / 追加 中央 `reports/124`（jsdom で framer-motion の完了コールバックが発火しない）、自 PJ `brain/reports/06`・tests 6 本、中央 `approaches/土台.md`（7 に質問の書式）・`brain_metrics.py`・README 指標節と思想節・`kickoff_questions.py`・`check_kickoff.py`・`開始時の質疑応答.md`・`指示書_報告書を事前の質問に変換.md`・TEMPLATE の事前の質問欄、自 PJ PREMISES の質疑応答表 / 更新 中央 `check_return_line.py`（参照した型の証拠）・`check_brain.sh`（再掲の警告）・approaches 10 本（土台参照）+ 4 本（事前の質問）・reports 10 件（事前の質問）・指示書・共通 CLAUDE.md、自 PJ history/README・CLAUDE.md・vitest 門・approaches 9 本 14 問（開いた形へ）・`approaches/配信先と実行環境を決める.md`（新規）・reports 113 件（事前の質問。全 123 件完了）・`approaches/画面と入力の設計で先に決めること.md`・`無料枠・プラン・費用の確認.md`（新規）／自 PJ `docs/検査と門.md`・`tests/docs-gates.test.ts`・`check_brain.sh`（焼き込みの警告）
