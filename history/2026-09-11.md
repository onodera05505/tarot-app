# 2026-09-11

## 1. 型の棚卸しと中央ブレインへの還元（`~/.claude/brain/指示書_型の棚卸しと還元.md` の実行）

**意図:** このプロジェクトの記録（history 4 日分・CLAUDE.md・brain 報告書 5 枚）から
「他のプロジェクトでも使える作業の型」を中央 `~/.claude/brain/` へ還し、以後も還り続ける
ように還元行の門を配線する。study-app と同じく、トラブル型（PWA が古い版・iOS 効果音・
擬似タイマー = 中央 77・78・97）は既に入っていたが、**作業の型は 1 本も無かった**。

**中央へ書いたもの（型のみ。値・固有名は落とした）:**
- 新規アプローチ 3 本: `止まっていたプロジェクトの再開`（08-21・09-02 の再開手順）、
  `Webアプリをストアへ出す着手前確認`（08-22・09-03 の公開準備と方針転換）、
  `体感の遅さの調査`（09-02 Phase D の計測手順）
- 既存へ統合 4 本: `仕様書駆動で挙動を変える`（2〜3 択で採否・本物同士の経路 1 本・契約の書き漏れは
  設計へ戻す。最初に入れた「仕様からテストを独立設計させる」は並行セッションが同日に後継へ畳んだため載せ直し）、`コンテンツ量産の進め方`（見本→味見→量産・
  合成方式・分野の一線・冒頭の汎用化を生成器で止める）、`不変条件を門に落とす`（集約は grep テスト、
  執筆ルールは生成器の検査）、`権限プロンプトを減らす運用`（一時許可は作業後に消す）
- 報告書 121: 畳んだはずのバックエンド URL が 200 を返す（`brain/reports/05` の型）

**自 PJ 側:**
- `brain/APPROACHES.md`・`brain/PREMISES.md`・`brain/approaches/` を配線（`setup_project.sh`、冪等）
- `brain/approaches/詳しく占うのカテゴリを追加する.md`（44 ベース + 198 アドバイス・見本は愚者・
  3 分担、など**このアプリの値**を集約）
- `brain/PREMISES.md` に決定・環境の癖・未決を history から拾って記入（appId、無料版フラグ、
  ストア申請保留、Node 24、tsconfig.test の罠、予告表示の未定義、裏面画像の解像度、など）

**門（以後も流れるように）:**
- `frontend/tests/history-return-line.test.ts` を新設。`history/YYYY-MM-DD.md`（2026-09-11 以降）の
  還元行「ブレイン: …」を検査し、無ければ `pnpm test` が落ちる = CI でデプロイも止まる。
  判定は中央 `check_return_line.py` と同じ正規表現（CI から中央スクリプトは届かないため、
  同じ判定をテストに持つ。直すときは両方を揃える旨をテストのコメントに記載）
- 変異テスト: 還元行の無い当日ファイルで vitest と中央スクリプトが落ちる → 行を足して通る、を確認
- `history/README.md` に還元行の規則、`CLAUDE.md` に着手時の APPROACHES/PREMISES 参照と
  締めの報告・history 末尾の還元行が必須である旨を追記

**主な変更ファイル:** `CLAUDE.md`、`history/README.md`、`brain/`（APPROACHES・PREMISES・
approaches/・README・reports/05）、`frontend/tests/history-return-line.test.ts`。
中央: `approaches/` 3 新規 + 4 更新、`reports/121`、`INDEX.md`、`APPROACHES.md`

**未解決課題:** Android Studio 導入 → 実機確認（本人）は継続。`brain/PREMISES.md` の未決欄を参照。

ブレイン: 参照した型 `指示書_型の棚卸しと還元.md`（棚卸し→突き合わせ→書く→門→報告） / 適用前に確認した値 無し（指示書は本人作、上限 8 本は指示書の値をそのまま適用） / 追加 中央 approaches 3 本（止まっていたプロジェクトの再開・Webアプリをストアへ出す着手前確認・体感の遅さの調査）+ reports/121、自 PJ approaches 1 本 + PREMISES / 更新 中央 approaches 4 本（仕様書駆動で挙動を変える・コンテンツ量産の進め方・不変条件を門に落とす・権限プロンプトを減らす運用）、自 PJ reports/05・README・history/README・CLAUDE.md
