# 2026-09-02

## 1. 再開時の棚卸し（調査のみ、コード変更なし）

**意図:** 8/22 以来の再開。残作業・権利/権限・開発に必要な要素の欠落を
確認し、改善が要るなら計画を立てる（ユーザー指示: まず調査分析から）。

**現状の確認結果:**
- 本番 `tarot-oracle-3qs.pages.dev` は 200、稼働版 `0822.2db63b8` = 最新
  コミット。ローカル main と origin/main は一致
- 基準線は緑: テスト 107 件・lint 0・本番ビルド成功（Node 24.13 / pnpm 10.32）
- 本番実行時依存の既知脆弱性: なし（`pnpm audit --prod`）。devDeps は 25 件
  （sharp/libvips 等、ビルド環境内。8/21 時点の 38 件から減少）
- ユーザーのリポジトリ整理: `brain/`（報告書 5 枚 + INDEX/README/TEMPLATE）
  新設、CLAUDE.md に「個人プロジェクト」宣言と brain 参照を追記。
  中央 brain の README にも本プロジェクトが「個人」として登録済み。
  **どちらも未コミット**（CLAUDE.md は変更、brain/ は untracked）

**権利（素材の出所）で見つかった欠落:**
1. **カード画像 22 枚 + 裏面の出所・ライセンスが記録されていない。** 目視では
   ライダー版の構図を踏襲した油彩調の AI 生成風。初回コミット（4/28）で
   一括追加され、`public/cards/README.md` は配置規約のみ。ストア公開時に
   「使用権を説明できる状態」が必要（生成ツールの利用規約・商用可否・
   プラン）。**ユーザーに出所を確認して記録する**（Phase A）
2. フォントは OS 同梱（Hiragino Mincho / Yu Mincho / system-ui）のみで
   外部フォント無し → 権利上の論点なし
3. リポジトリに LICENSE 無し（private なので現状は問題なし。公開する
   予定が無ければ不要）

**権限（設定・アカウント）で見つかった懸念:**
1. `.claude/settings.local.json`（gitignore 済み）に広すぎる恒久許可が残存:
   `security dump-keychain *`・`security find-internet-password *`・
   `Read(~/.ssh/**)`・`pkill -f "git.*push"`・`Read(~/develop/**)`。
   過去の認証トラブル対応時の一時許可が残ったもの。**削除を推奨**
2. 同ファイルに旧アカウントの URL（`backend.onodera-yudai.workers.dev`・
   `tarot-oracle-835.pages.dev`）が残存 → 掃除対象
3. **旧デプロイが 3 つとも公開中（200）:** `backend.onodera-development`
   （個人）・`backend.onodera-yudai`・`tarot-oracle-835.pages.dev`（社用側）。
   brain 05 の「未完了」のまま。Cloudflare 認証が要るためユーザー同席作業
4. `gh` CLI 未導入のためリポジトリの可視性は未検証（記憶では private）

**開発上のエッセンスの欠落・残骸（機能には影響なし）:**
- 未使用: `lottie-react` 依存、`src/assets/hero.png`・`react.svg`・`vite.svg`、
  `public/icons.svg`（いずれも参照 0）。`frontend/README.md` は Vite
  テンプレートのまま
- `backend/` は wrangler CLI のためだけに CI で install されている
  （Pages デプロイ用。frontend に wrangler を入れれば backend 依存を切れる）

**残作業（8/22 からの持ち越し、変更なし）:**
1. ユーザー: Apple Developer Program 加入（クリティカルパス）
2. 私: Capacitor 化（フェーズ 1。加入待ちと並行可）
3. /deep → /shuffle 遷移の数秒の引っかかり（磨き込み候補。未計測）
4. 旧 Worker/Pages の削除（上記）

**提案した改善計画（採否待ち。1 フェーズ = 1 コミット）:**
- Phase A（権利・権限の整理）: 画像出所の確認と記録 → `public/cards/README.md`
  に出所・ライセンス・生成条件を明記。settings.local.json の危険許可を削除。
  brain/ + CLAUDE.md をコミット
- Phase B（残骸掃除、内部品質）: 未使用依存・素材の削除、README の書き換え、
  CI の wrangler を frontend 側へ移して backend 依存を切る
- Phase C（旧デプロイ削除）: ユーザー同席で Cloudflare 3 件を削除、brain 05 を
  完了に更新
- Phase D（遷移の引っかかり）: まず実測（Performance 計測）してから直す
- その後 Capacitor 化へ

**主な変更ファイル:** `history/2026-09-02.md`（本記録のみ）

## 2. Phase A: 権利・権限の整理

**意図:** 棚卸し（1.）で見つかった欠落のうち、コードを触らず記録と設定で
閉じられるものを先に片づける。ユーザー採否済み。

**実施:**
- カード画像の出所を `frontend/public/cards/README.md` に記録（ユーザー回答:
  本人が Gemini Pro で独自プロンプトから生成、参照作品なし）。裏面の実体
  `major/000.webp` と、実ファイル名の表記ゆれ（`01_the_Magician`・
  `11_Justice`）も同 README に明記。ストア公開前に Google の生成 AI 規約の
  版を追記する旨を残した
- `.claude/settings.local.json`（git 管理外）の恒久許可を 90 件超 → 25 件に
  整理。キーチェーンのダンプ・`~/.ssh` 読取・`pkill`・`~/develop/**` 読取・
  旧アカウント URL への curl・一回限りのコミットメッセージ許可を削除
- ユーザーが整理した `brain/`（報告書 5 枚）と CLAUDE.md の「個人」宣言を
  本コミットに同梱

**未解決課題:** Phase B（残骸掃除）以降。`CARD_BACK` 定数が
`TodaysCard.tsx` と `CardReveal.tsx` に重複（共通ルール 3-1 違反）→ Phase B で集約

## 3. Phase B: 残骸掃除（内部品質、挙動変更なし）

**意図:** 棚卸しで見つけた参照ゼロの素材・依存と、CI の backend 依存を切る。
既存挙動はテスト 107 件で固定した状態で実施。

**実施:**
- 削除: `lottie-react` 依存、`src/assets/hero.png`・`react.svg`・`vite.svg`・
  `lottie/card-placeholder.json`、`public/icons.svg`（すべて参照 0 を grep で確認）
- 集約: 裏面画像パス `'/cards/major/000.webp'` が 3 画面に直書きされていた
  （TodaysCard / CardReveal / ShufflePage）→ `lib/api.ts` の `CARD_BACK_URL`
  に一本化。再発は `tests/card-back.test.ts` が検知（src 内の直書きが
  api.ts 以外に無いこと + 実体の存在）
- CI: wrangler を frontend の devDependencies（4.82.2、backend と同版に固定）へ
  移し、deploy.yml から backend の install ステップを撤去。**CI は backend/ に
  一切触らない**構成になった（backend/ は参照用残骸として残置。消すかは別判断）
- `frontend/README.md` を Vite テンプレートからプロジェクト説明に書き換え

**ハマった点:** 置換スクリプトで import 行まで `CARD_BACK`→`CARD_BACK_URL`
に置換し `CARD_BACK_URL_URL` を作ってテスト 10 件を落とした（自分のミス。
即修正）。

**検証:** テスト 109 件緑（+2）・lint 0・本番ビルド緑。**deploy.yml の変更は
次回 push の CI で実経路確認が必要**（wrangler の実行位置が変わった）

**未解決課題:** Phase C（旧デプロイ 3 件の削除、ユーザー同席）、
Phase D（/deep→/shuffle 遷移の実測）
