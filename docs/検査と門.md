# 検査と門 — 壊してはいけないことを、何が守っているかの表（2026-09-14 作成）

型は中央 `~/.claude/brain/approaches/不変条件を門に落とす.md` と `検査の層と門.md`。ここは**このアプリの現物**。
「状態」は あり／一部（迂回できる）／手順（人が守る規則だけ）／無し。**無しを隠さない**（無しの行がバックログ）。
この表が名指しするファイルの実在は `frontend/tests/docs-gates.test.ts` が見る（消したり改名したら表が嘘になる）。

## 1. 不変条件 → 門

| # | 不変条件（根拠） | 破るとどうなるか | 門 | 状態 |
| --- | --- | --- | --- | --- |
| 1 | 結果は完全ランダム。儀式の左右選択は正逆に影響しない（仕様 v3 §4.2、コミット 24efef6） | 演出が結果を左右し仕様違反 | `frontend/tests/components/ShufflePage.test.tsx`（左右選択が正逆を書き換えない） | あり |
| 2 | ストップを押しても deep（カテゴリ・回答）が消えない（v3 §5.7、`brain/reports/01_詳しく占うの結果が通常表示に落ちる.md`） | 詳しく占うが通常表示に落ちる | 同上（本物の ShufflePage でストップ後に deep 保持） | あり |
| 3 | 裏面画像のパスは `frontend/src/lib/api.ts` の CARD_BACK_URL だけが持つ（共通ルール 3-1） | 直書きが増え差し替え漏れ | `frontend/tests/card-back.test.ts`（src 内の直書き 0・実体あり） | あり |
| 4 | カードは 22 枚・連番・空欄なし・画像実体あり（v3 §4.1） | 一覧や結果が欠ける | `frontend/scripts/gen-cards.mjs`（書式違反で throw）+ `frontend/tests/cards-data.test.ts` | あり |
| 5 | 詳しく占うのデータは構造・字数・第一文のジャンル語を満たす（v3 §4.4、`brain/reports/04_ベース解釈の第一文にジャンル語が無い.md`） | 汎用解説風の個体が混ざる | `frontend/scripts/gen-deep.mjs`（落ちたら生成しない）+ `frontend/tests/deep-data.test.ts` | あり |
| 6 | 無料版では詳しく占うを完全非表示・`/deep` はリダイレクト・結果は通常表示（v3 §3.3） | 有料予定の機能が露出 | `frontend/tests/components/FreeRelease.test.tsx`・`frontend/tests/components/FreeRelease.enabled.test.tsx` | あり |
| 7 | 機能フラグの置き場は `frontend/src/lib/features.ts` の 1 箇所（v3 §3.3） | 切替漏れで Web とネイティブで挙動が割れる | 無し（直書きを grep するテストが無い。#3 と同じ形で固定できる） | 無し |
| 8 | ネイティブ内では Service Worker を登録しない。判定は `frontend/src/main.tsx` の 1 箇所 | SW の自動リロードとストア更新が二重になる | 無し（実機確認のみ） | 無し |
| 9 | 履歴は 100 件上限・新しい順・壊れ JSON でも落ちない（v3 §5.6） | 履歴画面が落ちる | `frontend/tests/history.test.ts` | あり |
| 10 | 本日の一枚は日付境界で切り替わり、同日は引き直さない（v3 §5.2） | 同じカードが翌日も出る | `frontend/tests/components/TodaysCard.test.tsx` | あり |
| 11 | 仕様書 v3 が一次資料。挙動を変える作業は 仕様→独立テスト設計→実装（共通ルール 2） | 文書と実装が乖離する | 無し（CLAUDE.md の規則のみ。spec-lint も §6 の差分検査も無い） | 手順 |
| 12 | `history/` の各日ファイル末尾に還元行「ブレイン: …」 | 学びが中央に還らない | `frontend/tests/history-return-line.test.ts` | あり |
| 13 | CI は test → build → deploy の順で、落ちたらデプロイしない | 壊れた版が本番へ出る | `.github/workflows/deploy.yml` | あり |
| 14 | 効果音は iOS で unlock してから鳴らす（中央 `reports/78`） | 実機で無音 | 無し（jsdom に Web Audio が無い。実機確認） | 無し |
| 15 | 積み直し後のカードの重なり順＝選択順（`brain/reports/02_カード1が他のカードの下に潜る.md`） | 選んだカードが潜る | 無し（見た目。演出を触ったら実機で見る） | 無し |
| 16 | 演出用と操作用の座標定数は別（`brain/reports/03_22枚化で山札が重なりはみ出す.md`） | 小さい端末でタップ対象がはみ出す | 無し（実機） | 無し |
| 17 | この表が名指しするファイルが実在する | 表が嘘になる | `frontend/tests/docs-gates.test.ts` | あり |

**バックログ（状態が無し・手順の行、効果と手間の比の順）:** #7（grep テスト 1 本で「あり」にできる）→ #11（仕様 §6 の差分を機械で見る案は未設計）→ #8・#14〜16（実機で見る項目として `brain/PREMISES.md` の未決に集約）。

## 2. 検査の層 → 門

| 層 | このアプリでの門 | 状態 |
| --- | --- | --- |
| 元データ（`frontend/data/cards.md`・`frontend/data/deep/*.md`） | gen-cards / gen-deep のバリデーション、cards-data / deep-data のテスト | あり |
| 投入（DB） | 該当せず（local-only。DB 無し） | — |
| API（`frontend/src/lib/api.ts`） | `frontend/tests/api.test.ts`（枚数・順序・重複なし・orientation・count） | あり |
| 描画（コンポーネント × 実データ、jsdom） | ShufflePage / DeepFlow / TodaysCard / FreeRelease | 一部（ResultPage・CardList・CardDetail・History の描画テストが無い） |
| 画面（本物のブラウザ × 状態 × 幅 × 配色） | 無し（状態の行列もスクリーンショットも無い。2026-09-02 の計測はスクラッチで一回限り） | 穴 |
| 使いやすさ | 無し（本人の実機レビューのみ） | 穴 |
| 運用（配信・更新の到達） | トップ右下の版表示で稼働版を特定、CI の順序 | 一部（Sentry のアラート設定と外形監視は未確認） |
| ネイティブ（Capacitor / Android） | 無し（APK 未ビルド。実機で見る項目は `brain/PREMISES.md`） | 穴 |

## 履歴
- 2026-09-14: 作成（開始時の質疑応答「不変条件を門に落とす」「検査の層と門」への回答として。門なし 6 行・穴 3 層を明記）
