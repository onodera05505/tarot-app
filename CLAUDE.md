# 作業ルール

**このプロジェクトは個人。** トラブル時はまず `brain/INDEX.md`（このリポジトリ）と
`~/.claude/brain/INDEX.md`（共通）を症状の言葉で grep する。解決したら報告書を1枚還す。

**トラブル・不具合に当たったら、調べ始める前に `brain/INDEX.md`（無ければ `~/.claude/brain/INDEX.md`）を読む。**

「共通ルール」はプロジェクト非依存で書いてある（philosophybook で
2026-08 に運用しながら固めたもの）。新しいプロジェクトへは共通ルールを
そのまま持っていき、「このプロジェクト固有」だけ書き直す。

## 共通ルール

### 1. 作業の規模を先に決める（コストの規律）

着手前に依頼を次の 3 つに分類し、**それより大きく動かない**。

| 規模 | 例 | 動き方 |
|---|---|---|
| 小 | 誤字・1 画面の不具合・文言変更 | 直近の履歴 1 日分だけ読み、触るファイルを最小に。ついでのリファクタや全体調査はしない |
| 中 | 機能追加・複数ファイルに跨る修正 | 関連する過去の設計判断を履歴で確認してから書く。影響する文書・画面内の説明文を同じ区切りで直す |
| 大（全体最適化） | **明示的に依頼されたときだけ** | まず調査だけ行い、課題を優先度順のフェーズに割って提示。1 フェーズ = 1 コミットの区切りで進め、区切りごとに検証を通す |

小・中の作業で大きな問題を見つけたら、**直さずに**履歴の未解決課題へ
記録して報告する。次の「大」の弾にする。

### 2. 仕様書が先、実装が後（仕様書駆動）

仕様書はプロジェクトの**「目指すべき形」を常に表す一次資料**であり、
実装は「仕様書との差分を消す作業」と位置づける（2026-08-21 採用。
コード先行だった従来方式では、駆け込み対応の 2 日で文書と実装が
乖離した実績がある。AI が書き手なら仕様先行の手間はほぼ無い）。

作業は 3 種類に分けて流れを決める:

| 種類 | 流れ |
|---|---|
| 挙動を変える（機能追加・仕様変更） | **① 仕様書の該当節を「変更後の文面」に直して採否を得る → ② 別コンテキストのエージェントが仕様の文面だけからテストを設計する → ③ 実装してテストを緑にする** → 仕様書・テスト・実装を同じコミットで確定 |
| 不具合修正 | 仕様書が正しく実装が違う状態。コードだけ直す（仕様書は触らない）。再発防止のテストは仕様書の文面を根拠に書く |
| 内部品質（リファクタ・性能・依存更新） | 仕様書に現れないのでコードのみ。挙動はテストで固定してから |

**② のテスト設計は実装者から独立させる**（2026-08-21 採用）。実装した
エージェントが自分でテストを書くと、実装の返す値を正解として写してしまい、
テストが仕様の検証でなく**実装の追認**になるため。テスト設計のエージェントには
**仕様書の該当文面だけを渡し、実装コードは見せない**。実装側はテストを
勝手に書き換えて緑にしてはならない——テストが間違っていると思ったら、
仕様書とテスト設計に差し戻す（緑にする都合でテストを曲げた瞬間、
この分離は無意味になる）。規模「小」では省略してよい。

**③ は緑になるまで自動で回してよい**（型・Lint・テスト・spec-lint。
2026-08-21 採用）。ただし**同じ失敗に 3 回連続で当たったら止めて報告する**——
それは実装の問題ではなく仕様かテストの矛盾の信号であり、リトライの
コストで磨り潰さない。**緑は完成ではなく、判定側の検証（5. 検証の基準）に
進む条件**。機械が見えない壊れ方（組版・UX・実経路）はループの外で確認する。

README・画面内の説明文・env やスキーマのコメントなど**従属する記述**も、
嘘になった瞬間のコミットで直す。「あとで直す」は直らない。

### 3. 手戻りを防ぐ 3 つの規律

過去に実際へ手戻りを生んだパターンの裏返し。

1. **同じものを 2 箇所に書かない。** 値・表記・ロジックを書き足す前に、
   既存の集約先を grep で探す。無ければ 2 箇所目を書く前に集約する。
   集約したら「必ずここを通すこと」をその場に書き、**テストでも固定する**
   （コメントだけでは再発する。集約の翌日に直書きが 7 箇所増えた実績がある）
2. **決定は記録し、黙って覆さない。** 設計判断は理由ごと履歴に残す。
   覆すときは当時の理由を読んでから、新しい根拠とセットで覆す
3. **測ってから言う。** 性能・原因の断定は実測の数字を出してから。
   「たぶんこれが原因」で直し始めない

### 4. 落とし穴はコードの現場に書く

ハマった制約（再起動が要る・この関数は分割禁止・この値は他と揃える、など）は
次に踏む人が見る場所＝**そのコードのコメント**に書く。このファイルには書かない。
既存のそうしたコメントは、邪魔に見えても消さない。

### 5. 検証の基準

- 型・Lint・テスト・本番ビルドを通してから区切る
- リファクタは、対象の挙動をテストで固定してから行う
- 実経路でしか壊れない箇所（アニメーション・PWA・外部連携など）は
  実経路で確認する。型が通る ≠ 動く
- 大きな作業では**作る主体と判定する主体を分ける**。実装した本人の
  自己申告で完成にせず、別コンテキスト（サブエージェント・検証スクリプト・
  実測）に反証させてから区切る。**テスト設計も判定側**であり、実装者では
  なく仕様書を入力とする別エージェントが行う（共通ルール 2 の ②）

### 6. 編集履歴（history/）— 指示されなくても自動で回す

- **編集を始める前**: 最新の日付ファイル（必要なら直近数日分）を読み、
  意図・設計判断・未解決課題を踏まえて作業に入る。読まずに変更を始めない。
  同一セッション内の再読み込みは不要。書式は `history/README.md`
- **作業の区切りごと**: `history/YYYY-MM-DD.md` に追記（無ければ新規作成、
  同日 2 回目以降は追記）。見出し + 意図 + 主な変更ファイル + 未解決課題。
  「今日の分を書いて」と言われるのを待たない
- 履歴は他のコード変更と一緒にコミットする

## このプロジェクト固有

- **構成:** モノレポ風 2 パッケージ。`frontend/`（React 19 + Vite + PWA。
  zustand / framer-motion / react-router / Sentry）と `backend/`
  （Hono on Cloudflare Workers + Prisma 7 + pg）。カードデータは
  現在フロントにバンドル（local-only。2026 年頭のコミット参照）
- **一次資料（仕様書駆動の起点）:** `docs/タロットアプリ_要件定義書_v3.md`
  （2026-08-21 全面改訂・実装と突き合わせ済み。実装との既知差分は同書 §6 に
  列挙してあり、挙動を変える作業はこの差分を消す方向で行う）。
  `backend/docs/` の v2 と PDF（20260427）は過去版の記録で、更新しない
- **コマンド:** frontend は `pnpm dev` / `pnpm build`（tsc -b 込み）/
  `pnpm lint`。backend は `pnpm dev`（wrangler）/ `pnpm deploy` /
  `pnpm cf-typegen`（Workers の型生成。バインディングを変えたら回す）
- **テスト:** frontend に vitest 導入済み（2026-08-21）。`pnpm test` /
  `pnpm test:watch`。テストは `frontend/tests/`（専用 tsconfig.test.json。
  `src/` は vite/client 型のため Node API を使うテストと分離してある）。
  CI でもビルド前に実行。backend は参照用残骸のためテスト無し
- **デプロイ:** `.github/workflows/deploy.yml` を参照
